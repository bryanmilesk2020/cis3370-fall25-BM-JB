This directory should contain all the configuration files for the tools you are
using. Create subdirectories for each tool (e.g., elasticsearch, logstash, suricata). This
makes it easy to version control your settings and replicate your environment.
